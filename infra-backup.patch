diff --git a/docker-compose.yml b/docker-compose.yml
index 4444444..5555555 100644
--- a/docker-compose.yml
+++ b/docker-compose.yml
@@
   pgadmin:
     image: dpage/pgadmin4:latest
     container_name: pgadmin
     restart: always
     environment:
       PGADMIN_DEFAULT_EMAIL: admin@sms.local
       PGADMIN_DEFAULT_PASSWORD: admin
     ports:
       - "5050:80"
     volumes:
       - pgadmin_data:/var/lib/pgadmin
     depends_on:
       - db
     networks:
       - smsnet
+
+  backup:
+    image: postgres:16
+    container_name: pgbackup
+    restart: always
+    environment:
+      - PGPASSWORD=postgres
+    volumes:
+      - postgres_backups:/backups
+    depends_on:
+      - db
+    networks:
+      - smsnet
+    entrypoint: >
+      sh -c '
+      echo "⏰ Starting scheduled PostgreSQL backups...";
+      while true; do
+        TS=$(date +%Y-%m-%d_%H-%M);
+        pg_dump -h db -U postgres -F c -f /backups/smsdb_$TS.dump smsdb &&
+        gzip /backups/smsdb_$TS.dump &&
+        find /backups -type f -mtime +7 -delete;
+        sleep 86400;
+      done
+      '
@@
 volumes:
   postgres_data:
   portainer_data:
   portainer_certs:
   pgadmin_data:
+
+  postgres_backups:
